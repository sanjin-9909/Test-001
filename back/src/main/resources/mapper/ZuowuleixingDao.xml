<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dao.ZuowuleixingDao">

	<!-- 可根据自己的需求，是否要使用 -->
    <resultMap type="com.entity.ZuowuleixingEntity" id="zuowuleixingMap">
        <result property="zuowuleixing" column="zuowuleixing"/>
        <result property="zhouqi" column="zhouqi"/>
    </resultMap>

	<select id="selectListVO"
		resultType="com.entity.vo.ZuowuleixingVO" >
		SELECT * FROM zuowuleixing  zuowuleixing         
        <where> 1=1 ${ew.sqlSegment}</where>
	</select>
	
	<select id="selectVO"
		resultType="com.entity.vo.ZuowuleixingVO" >
		SELECT  zuowuleixing.* FROM zuowuleixing  zuowuleixing 	
 		<where> 1=1 ${ew.sqlSegment}</where>
	</select>

    <select id="selectListView"
		resultType="com.entity.view.ZuowuleixingView" >

		SELECT  zuowuleixing.* FROM zuowuleixing  zuowuleixing 	        
        <where> 1=1 ${ew.sqlSegment}</where>
	</select>
	
	<select id="selectView"
		resultType="com.entity.view.ZuowuleixingView" >
		SELECT * FROM zuowuleixing  zuowuleixing <where> 1=1 ${ew.sqlSegment}</where>
	</select>
	


</mapper>
